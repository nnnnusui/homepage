---
applyTo: "**/*.ts,**/*.tsx"
---

## State Management

This project uses SolidJS. When a signal or store is needed to hold state, use `Wve` (`~/src/type/struct/Wve`) instead of SolidJS's built-in `createSignal` / `createStore`.
