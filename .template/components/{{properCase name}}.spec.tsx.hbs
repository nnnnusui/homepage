import "@testing-library/jest-dom";
import { render } from "@solidjs/testing-library";
import { describe, it, expect } from "vitest";

import styles from "./{{properCase name}}.module.styl";

import { {{properCase name}} } from "./{{properCase name}}";

describe("<{{properCase name}} /> test", () => {
  const { container } = render(() => <{{properCase name}} /> );
  const component = container.firstChild as HTMLElement;
  it("has rendered", async () => {
    expect(component).toBeInTheDocument();
  });
  it("has class", async () => {
    expect(component.classList.contains(styles.{{properCase name}})).toBe(true);
  });
});
